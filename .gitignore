node_modules
dist
dist-ssr
.DS_Store
*.local
.vite
.env

# Development-only design evidence (captures, build state). The brief and DESIGN.md sidecar stay tracked.
.impeccable/review/
.impeccable/build/
